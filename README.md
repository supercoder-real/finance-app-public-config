# Finance App Public Config

Public runtime config for language/theme settings and translation resources.

## Files
- `config/app-config.json`: supported languages/themes + defaults
- `i18n/<lang>/*.json`: translation namespaces (`common`, `auth`, `nav`, `budget`, `recurring`, `settings`)

This repo is intentionally public and contains no secrets.
